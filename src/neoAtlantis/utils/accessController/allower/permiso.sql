CREATE TABLE permiso_na(
    id_permiso  VARCHAR(50) NOT NULL,
    nombre      VARCHAR(50) NOT NULL,
    descripcion VARCHAR(255) NOT NULL,
    CONSTRAINT permiso_na_pk PRIMARY KEY (id_permiso)
);