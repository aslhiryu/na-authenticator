CREATE TABLE bitacora_na_data(
    fecha       DATETIME,
    usuario     VARCHAR(30),
    terminal    VARCHAR(60),
    origen      VARCHAR(30),
    evento      VARCHAR(1),
    detalle     VARCHAR(255)
);