CREATE TABLE bitacora_na(
    usuario     VARCHAR(30),
    terminal    VARCHAR(60),
    origen      VARCHAR(30),
    fecha       DATETIME,
    evento      VARCHAR(1),
    detalle     VARCHAR(255)
);